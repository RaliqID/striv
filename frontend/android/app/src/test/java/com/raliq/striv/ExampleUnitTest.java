package com.raliq.striv;

import static org.junit.Assert.*;

import org.junit.Test;

/**
 * Plain JVM unit test (no device required).
 *
 * A placeholder so the `test` source set exists and CI can run it immediately;
 * real logic belongs in the web layer, which is covered by the Next.js and
 * Laravel suites.
 */
public class ExampleUnitTest {

    @Test
    public void addition_isCorrect() {
        assertEquals(4, 2 + 2);
    }
}
