# Striv Frontend

Next.js 14 + TypeScript + Tailwind + shadcn/ui style.

## Setup

```bash
npm install
npm run dev
```

## Environment

`.env.local`:

```
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
```